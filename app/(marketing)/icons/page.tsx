import * as React from "react"
import { IconsView } from "@/components/site/icons-view"

export const metadata = {
  title: "Icons — Aurora design system",
  description:
    "The Aurora icon language: Lucide line icons (1.5–1.75px stroke, currentColor, never filled) plus the tone-coded Axon operation set on the same 24px grid.",
}

export default function IconsPage() {
  return <IconsView />
}
